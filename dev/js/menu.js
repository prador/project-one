import React from 'react'
import '../scss/App.scss'

let Menu = React.createClass({
            render() {
                return (<div> 
                            here is the menu
                        </div>)
            }
        })

export default Menu
